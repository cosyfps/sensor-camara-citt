{
  "[python]": {
    "editor.formatOnType": true,
    "editor.defaultFormatter": "ms-python.black-formatter"
  },

  "workbench.editor.showTabs": "single",
  "editor.minimap.enabled": false,
  "editor.scrollbar.vertical": "visible",
  "editor.scrollbar.verticalScrollbarSize": 10,
  "editor.overviewRulerBorder": false,
  "editor.hideCursorInOverviewRuler": true,
  "editor.formatOnSave": true, // crtl + shift + x = Prettier
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "[javascript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  },
  "editor.guides.indentation": true,
  "indenticator.width": 1,
  "indenticator.color.dark": "rgba(255,255,255,0.1)",
  "breadcrumbs.enabled": false,
  "editor.glyphMargin": false, // ctrl + k + t = One Dark Pro Mix / Tokyo Night
  "workbench.iconTheme": "material-icon-theme",
  "explorer.confirmDelete": false,
  "files.autoSave": "afterDelay",
  "python.analysis.typeCheckingMode": "basic",
  "git.autofetch": true,
  "git.confirmSync": false,
  "liveServer.settings.donotShowInfoMsg": true,
  "terminal.integrated.env.windows": {},
  "diffEditor.ignoreTrimWhitespace": false,
  "workbench.tree.enableStickyScroll": true,
  "livePreview.notifyOnOpenLooseFile": false,
  "editor.fontSize": 15,
  "files.associations": {
    "*.html": "html"
  },
  "console-ninja.featureSet": "Community",
  "symbols.hidesExplorerArrows": false,
  "tabnine.experimentalAutoImports": true,
  "window.titleBarStyle": "custom",
  "workbench.colorTheme": "Tokyo Night",
  "workbench.productIconTheme": "fluent-icons",
  "workbench.activityBar.location": "top",
  "explorer.confirmDragAndDrop": false,
  "explorer.fileNesting.patterns": {
    "*.ts": "${capture}.js",
    "*.js": "${capture}.js.map, ${capture}.min.js, ${capture}.d.ts",
    "*.jsx": "${capture}.js",
    "*.tsx": "${capture}.ts",
    "tsconfig.json": "tsconfig.*.json",
    "package.json": "package-lock.json, yarn.lock, pnpm-lock.yaml, bun.lockb",
    "*.sqlite": "${capture}.${extname}-*",
    "*.db": "${capture}.${extname}-*",
    "*.sqlite3": "${capture}.${extname}-*",
    "*.db3": "${capture}.${extname}-*",
    "*.sdb": "${capture}.${extname}-*",
    "*.s3db": "${capture}.${extname}-*"
  },
  "[html]": {
    "editor.defaultFormatter": "vscode.html-language-features"
  } // ctrl + shift + x = Material Icon Theme
}

// ctrl + shift + x = Material Icon Theme *Philipp Kief
// ctrl + shift + x = Fluent Icons *Miguel Solorio
// ctrl + shift + x = Better Comments *Aaron Bond
// ctrl + shift + x = Error Lens *Alexander
// ctrl + shift + x = Prettier *Prettier
// ctrl + shift + x = Theme: One Dark Pro *binaryify
// ctrl + shift + x = Theme: Tokyo Night *enkia
// ctrl + shift + x = Git Extension Pack *Don Jayamanne
// ctrl + shift + x = HTML CSS Support *ecmel
// ctrl + shift + x = HTML Boilerplate *sidthesloth
// ctrl + shift + x = Image Preview *Kiss Tamas
// ctrl + shift + x = CSS Peek *Pranay Prakash
// ctrl + shift + x = Boostrap Class Autocomplete *torresgol10
// ctrl + shift + x = Live Server *Ritwick Dey
// ctrl + shift + x = IntellinSense for CSS class namesin HTML *Zignd
// ctrl + shift + x = Ionic *ionic
// ctrl + shift + x = Django Support *Al Mahdi
// ctrl + shift + x = SQLite Viewer *Florian Klampfer
// ctrl + shift + x = Extension Pack for Java *Microsoft
// ctrl + shift + x = Python *Microsoft
// ctrl + shift + x = Python Debugeger *Microsoft
// ctrl + shift + x = Pylance *Microsoft
// ctrl + shift + x = isort *Microsoft
// ctrl + shift + x = Black Formatters *Microsoft
// ctrl + shift + x = Snappify *snappify
// ctrl + shift + x = Code Runner *Jun Han
// ctrl + shift + x = Draw.io *Henning Dieterichs
// ctrl + shift + x = Excel Viewer *GrapeCity
// ctrl + shift + x = Tabnine AI *Tabnine